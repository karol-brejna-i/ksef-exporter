Now, only basic attributes are extracted from XML during import.

More advanced searches and operations will soon be required, so more attributes should be exposed.

For now, here are the candidates:
- DoZaplaty, SumaObciazen, SumaOdliczen